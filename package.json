{
  "name": "green-yoga",
  "private": true,
  "description": "Website for the yoga studio Green Yoga in Berlin",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "build": "gatsby build",
    "develop": "gatsby develop",
    "format": "prettier --write src/**/*.{js,jsx}",
    "start": "npm run develop",
    "serve": "gatsby serve",
    "deploy": "gatsby build --prefix-paths && gh-pages -d public",
    "test": "echo \"Write tests! -> https://gatsby.dev/unit-testing\""
  },
  "dependencies": {
    "gatsby": "^4.3.0",
    "gatsby-background-image": "^1.5.3", // Check if there's a newer version or alternative, as this package might be outdated
    "gatsby-plugin-image": "^2.0.0", // Replacing gatsby-image
    "gatsby-plugin-react-helmet": "^5.0.0",
    "gatsby-plugin-sharp": "^4.2.0",
    "gatsby-source-filesystem": "^4.2.0",
    "gatsby-transformer-json": "^4.2.0",
    "gatsby-transformer-remark": "^2.16.1", // Corrected version
    "gatsby-transformer-sharp": "^4.2.0",
    "intersection-observer": "^0.12.0", // This is the latest version as of my last update
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-helmet": "^6.1.0"
  },
  "devDependencies": {
    "gh-pages": "^3.3.0",
    "prettier": "^2.5.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Chrysalis-B/green-yoga.git"
  }
}
